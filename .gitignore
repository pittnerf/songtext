# Python
__pycache__/
*.py[cod]
.venv/

# PyInstaller
build/
dist/
build_songs.exe

# Optional: keep source PDFs out of git (keep them locally for rebuilds)
# songs_pdfs/*.pdf

# OS / editor
.DS_Store
.vscode/

# Real Firebase values — never commit this file.
# Copy public/js/config.example.js to config.js for local testing only.
public/js/config.js
